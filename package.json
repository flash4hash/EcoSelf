{
  "name": "ecoself",
  "version": "1.0.0",
  "description": "EcoSelf - Carbon Footprint Awareness Platform for Indian Users",
  "main": "server/index.js",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server/index.js",
    "dev": "concurrently \"cd client && npm run dev\" \"cd server && node index.js\"",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "test": "npm run test --prefix client",
    "test:calc": "cd client && node src/utils/calculateFootprint.test.js",
    "test:api": "node server/tests/api.test.js",
    "test:validators": "node server/tests/validators.test.js",
    "test:factors": "cd client && node src/utils/emissionFactors.test.js",
    "test:edge": "cd client && node src/utils/edgeCases.test.js",
    "test:all": "npm run test && npm run test:validators"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
